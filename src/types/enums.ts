export enum Paths {
    HOME = "/",
    ABOUT = "/about",
    COURSE = "/courses",
    GALLERY = "/gallery",
    CONTACT = "/contact"
}

export enum Nav {
    HOME = "Home",
    ABOUT = "About",
    COURSE = "Courses",
    GALLERY = "Gallery",
    CONTACT = "Contact"
  }  